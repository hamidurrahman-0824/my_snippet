(-u sets upstream so future pushes can just be git push).. means “add all changes in this folder.”bashcd path/to/your/projectCheck statusCommit changesCopy the repo URL (HTTPS or SSH).Create a repo on GitHubDaily workflow (add, commit, push)Example: "Added PrimeIter class with fixes"First push may require:git add .git commit -m "Meaningful commit message"git initgit push -u origin maingit push origin maingit remote add origin <your-repo-URL>git statusGo to GitHub → click New repository → give it a name (e.g., clean-code-practice).If your branch is called master, use master instead of main.Initialize Git locallyOne-time setup (per repository)Push to GitHubStage changesThis shows which files are modified or new.Whenever you’ve made changes and want to upload them:You can also add specific files: git add file.py.