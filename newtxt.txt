1.git fetch origingit repository is out of sync(diverged history):"git push" may be rejected, or "git pull" may not merge correctly.if changes are made on both pcs without pulling first, the branches may diverge.then4.set proper line endings:"git push" may be rejected, or "git pull" may not merge correctly.git commit -m "message"2.always do thisline ending issues(wind vs lin):line ending issues(wind vs lin):windows uses crlf (/r/n) and linux uses lf(/n). if core.autocrlf is not set properly, git might think files different evengit pushwhen they're the same. this can cause "phantom" changes or failure t update.when they're the same. this can cause "phantom" changes or failure t update.3.git pull origin <branch_name>uncommitted changes1. check your branchif either pc has local cuncommitted changes, git may prevent pulling/pushing until those changes are resolved.5. caching or shallow clone issues:4.branch confusion3.maybe on pc is on a diffrent branch than the other E.G. linux on main windows on master or a fewture branch is being usedon both pcs:5. caching or shallow clone issues:git repository is out of sync(diverged history):rare but if the close was shalllow or cache issues exist "git pull" may not fetch al updatesgit add .##fixwin1. check your branchlinon both pcs:@git status2.git branchgit commit -m "Your message"git log --oneline --graph --decorate --all -5if either pc has local cuncommitted changes, git may prevent pulling/pushing until those changes are resolved.2.Pull latest changes before pushing3.Push changesalways do thisuncommitted changesgit fetch origingit push origin <branch_name>git statusgit log --oneline --graph --decorate --all -5git pull origin <branch_name>windows uses crlf (/r/n) and linux uses lf(/n). if core.autocrlf is not set properly, git might think files different even3.Push changesgit statusgit add .##git pull → edit → git add → git commit → git pushgit commit -m "Your message"git config --global core.autocrlf truegit push origin <branch_name>1.4.set proper line endings:##fixwin##git add <files>git config --global core.autocrlf truegit log --oneline --graph --decorate -5lin2.Pull latest changes before pushinggit config --global core.autocrlf inputrare but if the close was shalllow or cache issues exist "git pull" may not fetch al updates##git add <files>4.branch confusiongit commit -m "message"git pull --rebase → edit → git add → git commit → git pushgit pushif changes are made on both pcs without pulling first, the branches may diverge.thengit config --global core.autocrlf input##git pull → edit → git add → git commit → git pushgit branch@maybe on pc is on a diffrent branch than the other E.G. linux on main windows on master or a fewture branch is being usedgit status