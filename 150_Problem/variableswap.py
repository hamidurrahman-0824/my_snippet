import time
class Healthy:
	def __init__(self,name,age):
		self.name = name
		self.age = age
	def __str__(self):
		return f"Name {self.name}, Age {self.age}"
	def __repr__(self):
		return f"(name ='{self.name}'), (age = '{self.age})'"
	def will_be_valid(self):
		return 
	def is_valid(self):
		if self.
		return self.age>18
	
personell = Healthy('Majed',29)
print(personell.is_valid())
